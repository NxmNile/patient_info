// server.js removed — this project now uses Next.js built-in server (frontend-only)
// If you previously used a custom Express/socket server, it's been disabled.
// Start the app with `npm run dev` which now runs `next dev`.
